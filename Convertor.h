#pragma once

#include <string>
#include <iostream>
using namespace std;
class Convertor
{
public:
	string sz_Convert (size_t MAX_CHARS, char x);
	bool check_number (size_t MAX_CHARS, string x);
};

